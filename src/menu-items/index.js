// project import
import pages from './pages';
import dashboard from './dashboard';
import administration from './administration';
import support from './support';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, pages, administration, support]
};

export default menuItems;
